#include <iostream>
#include <stream>
#include <std>

int add (int a, int b){
    int a = 0, b = 0; 
    cout << a + b;
}

float tax(int sum, int per){ // int of sum of money, int of percentage 
    int cTax = 0, sTax = 0; //County tax state Tax
    int sum = 0; // money paid
    float percent = (cTax + sTaxi) / 100;
    float total = percent * sum;
    return(total);
}

float revenue(int sales, int per){ // amount of sales, and the percentage
    int sales = 0; 
    int per = 0;
    float dotpercent = (per / 100);
    float rev = sales * dotpercent;
    return(rev)
}


